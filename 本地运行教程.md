# 🚀 本地运行完整教程（零基础版）

这是一份非常详细的教程，即使你完全不懂编程也能跟着操作！

---

## 📋 第一步：安装必要软件

### 1.1 安装 Node.js

**什么是 Node.js？**
Node.js 是运行这个博客项目必需的环境（就像手机需要操作系统一样）。

**下载和安装：**

1. 打开浏览器，访问：https://nodejs.org/zh-cn
2. 你会看到两个绿色按钮，选择 **左边的按钮**（LTS 版本，推荐版本）
3. 下载完成后，双击安装包
4. 一路点击 "下一步" 或 "Next"，全部使用默认设置
5. 安装完成后，**重启电脑**（这很重要！）

**验证安装成功：**

重启电脑后：

- **Windows 用户**：按 `Win + R`，输入 `cmd`，按回车
- **Mac 用户**：按 `Command + 空格`，输入 `terminal`，按回车

在打开的黑色窗口（叫"终端"或"命令提示符"）中输入：

```bash
node --version
```

如果显示类似 `v20.11.0` 这样的版本号，说明安装成功！✅

---

## 📦 第二步：下载并解压项目

### 2.1 下载项目文件

1. 从我提供的链接下载 `wisp-clone-blog-fixed.tar.gz` 文件
2. 把文件保存到一个你容易找到的地方，比如 **桌面** 或 **文档** 文件夹

### 2.2 解压文件

**Windows 用户：**
1. 右键点击 `wisp-clone-blog-fixed.tar.gz`
2. 选择 "解压到当前文件夹" 或 "Extract Here"
3. 如果没有这个选项，需要先安装解压软件：
   - 下载 7-Zip：https://www.7-zip.org/
   - 安装后再右键解压

**Mac 用户：**
1. 双击 `wisp-clone-blog-fixed.tar.gz` 文件
2. 会自动解压出一个名为 `wisp-clone-blog-fixed` 的文件夹

解压后，你应该看到一个名为 `wisp-clone-blog-fixed` 的文件夹。

---

## 💻 第三步：打开终端/命令提示符

### 3.1 进入项目文件夹

**Windows 用户：**

方法一（简单）：
1. 打开 `wisp-clone-blog-fixed` 文件夹
2. 在文件夹空白处，**按住 Shift 键 + 右键点击**
3. 选择 "在此处打开 PowerShell 窗口" 或 "在此处打开命令窗口"

方法二（手动）：
1. 按 `Win + R`，输入 `cmd`，按回车
2. 输入以下命令（假设项目在桌面）：
```bash
cd Desktop\wisp-clone-blog-fixed
```

**Mac 用户：**

方法一（简单）：
1. 打开 Finder，找到 `wisp-clone-blog-fixed` 文件夹
2. 打开 "终端" 应用
3. 把文件夹直接拖到终端窗口中
4. 在拖入的路径前面加上 `cd `（cd 后面有一个空格）
5. 按回车

方法二（手动）：
1. 按 `Command + 空格`，输入 `terminal`，打开终端
2. 输入以下命令（假设项目在桌面）：
```bash
cd ~/Desktop/wisp-clone-blog-fixed
```

**验证是否进入正确目录：**

输入以下命令：

**Windows:**
```bash
dir
```

**Mac/Linux:**
```bash
ls
```

你应该看到一些文件夹和文件，包括：
- `app`
- `components`
- `content`
- `package.json`
- `README.md`

如果看到这些，说明你在正确的位置！✅

---

## 🔧 第四步：安装项目依赖

### 4.1 什么是依赖？

依赖就是这个项目需要的其他代码库（就像手机应用需要下载才能使用）。

### 4.2 开始安装

在终端中输入以下命令：

```bash
npm install
```

**然后按回车键！**

**会发生什么：**
- 终端会开始下载很多文件
- 你会看到很多文字在滚动
- 可能会出现一些黄色的 "WARN" 警告（**这是正常的，不用担心！**）
- 整个过程大约需要 **1-3 分钟**，具体取决于你的网速

**安装完成的标志：**
- 看到光标重新出现，可以输入命令了
- 项目文件夹中出现了一个名为 `node_modules` 的新文件夹

如果安装很慢或失败，试试这个命令（使用国内镜像）：

```bash
npm install --registry=https://registry.npmmirror.com
```

---

## 🎉 第五步：启动博客（最激动人心的时刻！）

### 5.1 运行开发服务器

在终端中输入：

```bash
npm run dev
```

**然后按回车键！**

**你会看到：**
```
  ▲ Next.js 15.0.3
  - Local:        http://localhost:3000
  - Network:      http://192.168.x.x:3000

 ✓ Ready in 2.3s
```

当你看到 "✓ Ready" 和一个网址时，说明成功了！🎉

### 5.2 在浏览器中查看

1. 打开你的浏览器（Chrome、Edge、Safari 等）
2. 在地址栏输入：
```
http://localhost:3000
```
3. 按回车

**你应该看到：**
- 一个漂亮的博客首页
- 有几篇示例文章
- 右上角有深色/浅色主题切换按钮

**恭喜！你的博客已经在本地运行了！**🎊

---

## 🎨 第六步：查看和测试功能

### 6.1 测试博客功能

现在你可以：

✅ **浏览文章**
- 点击任何文章卡片
- 查看完整文章内容

✅ **切换主题**
- 点击右上角的月亮/太阳图标
- 看看深色模式和浅色模式

✅ **点击分类**
- 点击顶部导航栏的分类（Travel、Food 等）
- 查看该分类下的所有文章

✅ **点击标签**
- 在文章底部点击标签
- 查看带有该标签的所有文章

✅ **测试分享按钮**
- 在文章页面滚动到底部
- 看看 8 个不同的分享按钮
- 试试"复制链接"按钮

✅ **响应式设计**
- 调整浏览器窗口大小
- 看看页面如何适应不同屏幕

### 6.2 停止服务器

当你想停止博客运行时：

1. 回到终端窗口
2. 按 `Ctrl + C`（Windows 和 Mac 都一样）
3. 看到提示后，输入 `y` 并按回车（或直接按回车）

---

## 📝 第七步：添加你自己的文章

### 7.1 删除示例文章

1. 用文件管理器打开项目文件夹
2. 找到 `content/posts/` 文件夹
3. 删除里面的所有 `.md` 文件（这些是示例文章）

### 7.2 创建新文章

1. 在 `content/posts/` 文件夹中
2. 创建一个新的文本文件
3. 命名规则：
   - 使用英文小写字母
   - 用连字符 `-` 分隔单词
   - 以 `.md` 结尾
   - 例如：`my-first-post.md`

### 7.3 文章模板

用记事本或任何文本编辑器打开新文件，复制以下模板：

```markdown
---
title: "我的第一篇文章"
date: "2024-11-28"
excerpt: "这是我的第一篇博客文章，很开心！"
coverImage: "https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=1200"
author: "你的名字"
category: "生活"
tags: ["第一篇", "测试"]
---

# 欢迎来到我的博客

这是我的第一篇文章！

## 第一个小标题

在这里写你想写的内容。

**粗体文字**
*斜体文字*

- 列表项 1
- 列表项 2
- 列表项 3

> 这是一个引用块

## 第二个小标题

继续写更多内容...
```

### 7.4 保存并查看

1. 保存文件
2. 回到浏览器
3. 刷新页面（按 `F5` 或 `Command + R`）
4. 你应该看到新文章出现了！

---

## 🔧 常见问题解决

### 问题 1："npm 不是内部或外部命令"

**原因：** Node.js 没有正确安装
**解决：**
1. 重新安装 Node.js
2. 安装后重启电脑
3. 重新打开终端

### 问题 2：端口 3000 被占用

**错误信息：** "Port 3000 is already in use"
**解决：** 换个端口运行
```bash
npm run dev -- -p 3001
```
然后访问 `http://localhost:3001`

### 问题 3：安装依赖很慢或失败

**解决：** 使用国内镜像
```bash
npm install --registry=https://registry.npmmirror.com
```

### 问题 4：修改文章后没有变化

**解决：**
1. 保存文件
2. 回到浏览器
3. 按 `Ctrl + F5`（强制刷新）

### 问题 5：页面显示乱码

**原因：** 文件编码问题
**解决：**
1. 用更好的编辑器（推荐 VS Code）
2. 保存时选择 "UTF-8" 编码

---

## 🎯 推荐工具

为了更好地编辑文章，推荐使用以下工具：

### Visual Studio Code（强烈推荐）
- 免费、功能强大的代码编辑器
- 下载：https://code.microsoft.com/
- 支持 Markdown 预览
- 自动保存功能

**使用方法：**
1. 安装 VS Code
2. 用 VS Code 打开项目文件夹
3. 左侧会显示所有文件
4. 点击任何文件进行编辑

---

## 📚 文章写作技巧

### Markdown 语法速查

```markdown
# 一级标题
## 二级标题
### 三级标题

**粗体文字**
*斜体文字*

[链接文字](https://example.com)

![图片描述](图片网址)

- 无序列表项
- 无序列表项

1. 有序列表项
2. 有序列表项

> 引用文字

`代码`

```代码块```
```

### 封面图片推荐

免费高质量图片网站：
- Unsplash：https://unsplash.com/
- Pexels：https://www.pexels.com/
- Pixabay：https://pixabay.com/

使用方法：
1. 找到喜欢的图片
2. 复制图片链接
3. 粘贴到文章的 `coverImage` 字段

---

## 🎓 下一步学习

### 1. 自定义网站信息

编辑 `app/layout.tsx` 文件：
- 修改网站标题
- 修改网站描述

### 2. 修改分类

编辑 `components/header.tsx` 文件：
- 找到 `categories` 数组
- 添加或删除分类

### 3. 修改主题颜色

编辑 `app/globals.css` 文件：
- 查找 CSS 变量
- 修改颜色值

---

## ✅ 成功检查清单

- [ ] Node.js 安装成功
- [ ] 项目文件解压完成
- [ ] 依赖安装成功（有 node_modules 文件夹）
- [ ] 能够运行 `npm run dev`
- [ ] 浏览器能访问 localhost:3000
- [ ] 能看到博客首页
- [ ] 能点击文章查看详情
- [ ] 能切换深色/浅色主题
- [ ] 分类和标签能正常点击
- [ ] 分享按钮能正常使用

如果所有项都打勾了，恭喜你！你已经成功掌握了本地运行博客！🎉

---

## 🆘 需要帮助？

如果遇到任何问题：

1. **仔细阅读错误信息**（虽然看起来很复杂，但通常会告诉你哪里出错了）
2. **检查是否按步骤操作**（有时候跳过一步就会出问题）
3. **重启终端和浏览器**（很多时候重启就能解决）
4. **重新安装依赖**（删除 node_modules 文件夹，重新运行 `npm install`）

---

## 🎊 恭喜你！

你已经学会了：
- ✅ 安装 Node.js
- ✅ 解压项目文件
- ✅ 使用终端命令
- ✅ 安装项目依赖
- ✅ 运行开发服务器
- ✅ 在浏览器中查看博客
- ✅ 添加自己的文章

现在你可以开始写博客了！加油！📝✨
